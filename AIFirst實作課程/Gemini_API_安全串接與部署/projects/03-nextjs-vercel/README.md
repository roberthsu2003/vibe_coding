# 專案三：Next.js on Vercel

**目標**：**同一份 repo** 內含頁面與 **Route Handler／API Route**；瀏覽器只載入前端，**`app/api` 或 `pages/api`** 在伺服器端讀 `GEMINI_API_KEY` 再呼叫 Gemini。部署 **Vercel**，金鑰設於 **Vercel 環境變數**。

- 上層說明：[`../../README.md`](../../README.md)
- 請於此資料夾建立 Next.js 專案與 `.env.example`（變數名即可，不含真鑰）。
