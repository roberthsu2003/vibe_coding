# 猜數字遊戲 - Prompt 參考

> **重要提示**：本專案使用 Vite、React 和 TypeScript 開發。建議學員先安裝 Node.js 環境（並使用 `npm create vite@latest` 建立專案），或直接使用線上編輯器（如 CodeSandbox、StackBlitz）來執行專案。

## 遊戲簡介
猜數字遊戲是一個經典的邏輯推理遊戲。電腦隨機產生一個數字，玩家需要透過提示（太大、太小）來猜出正確的數字。

## 迭代階段 1：基礎版本

### Prompt 範例
```
請幫我建立一個猜數字遊戲，使用 Vite、React 和 TypeScript。

基本需求：
1. 建立一個 React 元件，包含輸入框和按鈕
2. 使用 Math.random() 隨機產生一個 1-100 之間的數字，並透過 React State 管理遊戲狀態
3. 玩家在輸入框中輸入猜測的數字，點擊按鈕提交
4. 根據玩家的猜測給予提示：
   - 如果猜的數字太大，顯示「太大了！」
   - 如果猜的數字太小，顯示「太小了！」
   - 如果猜對了，顯示「恭喜！猜對了！」
5. 記錄猜測次數
6. 猜對後顯示總共猜了幾次
7. 提供「再玩一次」按鈕重新開始遊戲

請使用清晰的元件結構，加上 TypeScript 型別定義與註解說明，並使用 CSS 美化介面。
```

---

## 迭代階段 2：增加難度選擇與範圍設定

### Prompt 範例
```
在現有的猜數字遊戲基礎上，請增加以下功能：

1. 難度選擇：
   - 簡單：1-50 的範圍，最多 10 次機會
   - 中等：1-100 的範圍，最多 7 次機會
   - 困難：1-200 的範圍，最多 5 次機會

2. 自訂範圍：
   - 允許玩家自訂數字範圍（例如：1-1000）
   - 根據範圍大小自動計算允許的猜測次數

3. 機會限制：
   - 如果猜測次數超過限制，遊戲結束並顯示正確答案
   - 顯示剩餘機會數

4. 遊戲統計：
   - 記錄最佳成績（最少猜測次數）
   - 記錄總遊戲次數
   - 顯示平均猜測次數
```

---

## 迭代階段 3：圖形化介面

### Prompt 範例
```
請為猜數字遊戲建立更美觀的圖形化介面，使用 React 元件化設計：

1. 遊戲介面設計：
   - 使用 CSS 或樣式套件設計遊戲標題和整體佈局
   - 顯示當前難度或範圍
   - 顯示剩餘機會數

2. 輸入介面：
   - 數字輸入框（實作為受控元件 Controlled Component）
   - 「猜測」按鈕
   - 或使用 range input（滑桿）來選擇數字

3. 提示顯示：
   - 清楚顯示「太大」或「太小」的提示（使用不同顏色）
   - 顯示猜測歷史（已猜過的數字列表）
   - 用視覺化方式顯示數字範圍的縮小過程（例如使用進度條或數線）

4. 遊戲狀態：
   - 開始畫面（選擇難度元件）
   - 遊戲中畫面（主要遊戲元件）
   - 遊戲結束畫面（顯示結果、最佳成績元件）
```

---

## 迭代階段 4：增加提示功能與策略建議

### Prompt 範例
```
請在猜數字遊戲中增加提示功能和策略建議：

1. 提示功能（可選）：
   - 「提示」按鈕：顯示可能的數字範圍
   - 「建議」按鈕：根據當前範圍建議一個最佳猜測數字（二分搜尋策略）

2. 猜測歷史：
   - 顯示所有猜過的數字
   - 用顏色標記：太大（紅色）、太小（藍色）
   - 顯示每次猜測後縮小的範圍

3. 策略教學：
   - 遊戲結束後顯示使用的策略分析
   - 建議最佳策略（二分搜尋法）
   - 顯示如果使用最佳策略需要幾次猜測

4. 視覺化範圍：
   - 用進度條或數線顯示當前可能的數字範圍
   - 標記已猜過的數字位置
```

---

## 迭代階段 5：多人模式與競賽功能

### Prompt 範例
```
請在猜數字遊戲中增加多人模式和競賽功能：

1. 多人輪流模式：
   - 兩個玩家輪流猜測
   - 先猜對的玩家獲勝
   - 記錄每個玩家的猜測次數

2. 競賽模式：
   - 多個玩家同時進行遊戲（各自猜自己的數字）
   - 比較誰用最少的次數猜對
   - 顯示排行榜

3. 電腦對手模式：
   - 玩家可以選擇與電腦對戰
   - 電腦使用最佳策略（二分搜尋）
   - 比較玩家和電腦的表現

4. 記錄與統計：
   - 記錄多人遊戲的勝負記錄
   - 顯示對戰歷史
   - 計算勝率
```

---

## 技術建議

- **環境建置**：建議使用 `npm create vite@latest game -- --template react-ts` 快速建立專案
- 使用 **React State (`useState`)** 來管理數字範圍、猜測次數、歷史紀錄等狀態
- 將畫面拆分為多個獨立的 **React 元件 (Components)**（例如：`StartScreen`、`GameBoard`、`ResultPanel`）
- 使用 **TypeScript** 定義 Props 和狀態的型別 (`interface` / `type`)
- 使用 **`localStorage`** 搭配 `useEffect` 來儲存遊戲記錄和最佳成績
- 介面排版可搭配 **CSS Modules**、**Tailwind CSS** 或一般 CSS，並加入過渡動畫提升體驗

## 學習重點

1. **階段1**：學習 React 狀態管理 (`useState`)、事件處理 (Event Handling)、基礎 TypeScript 型別
2. **階段2**：學習複雜狀態維護、條件渲染 (Conditional Rendering)
3. **階段3**：學習元件拆分 (Component Composition)、Props 傳遞、表單受控元件 (Controlled Components)
4. **階段4**：學習進階邏輯實作、陣列操作、自訂 Hook (Custom Hooks) 封裝共用邏輯
5. **階段5**：學習更複雜的資料結構設計、狀態同步與組件間的溝通
