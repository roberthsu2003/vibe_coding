# 雲端智慧庫存管理系統 📊 (密碼驗證安全版)

本單元將帶領大家完成一個非常實用的**雲端智慧庫存管理系統**！我們將結合 **Google Sheets（Google 試算表）** 作為雲端資料庫，並使用 **Google Apps Script (GAS)** 部署一個具備自動計算、ID 遞增與安全**密碼驗證**防護的後端 API。

最後，我們會配合精心設計的單一網頁前端檔案 (`index.html`)，採用現代**毛玻璃美學 (Glassmorphism)** 與**暗黑/亮光主題切換**，讓您無需安裝任何軟體與環境，雙擊檔案即可在本地直接連線操作！

---

## 📅 課程架構與步驟流程

1. **第一階段：資料庫準備**（在雲端硬碟建立並匯入庫存資料）
2. **第二階段：部署雲端 API 後端**（複製 Apps Script 並自訂安全密碼，發布為網頁應用程式）
3. **第三階段：使用 Google AI Studio 生成前端**（複製 Prompt 並透過 AI 一鍵生成網頁）
4. **第四階段：執行極致美感前端**（本地雙擊打開網頁，輸入網址與驗證密碼即刻啟動）

---

## 🛠️ 第一階段：資料庫準備

這部分需要正確設定 Google 試算表，請按步驟仔細操作：

### 步驟 1：建立並設定 Google 試算表 (Google Sheets)
1. 前往 [Google 雲端硬碟](https://drive.google.com/)，建議先**建立一個專屬資料夾**（例如：`AIFirst_庫存管理`）並進入。
2. 點擊左上角「**新增**」 > 「**Google 試算表**」，並將左上角的檔案名稱命名為「**庫存管理系統**」。
3. **匯入現有庫存資料**：
   - 點擊上方選單的 **「檔案」 > 「匯入」**。
   - 選擇「**上傳**」，將本資料夾下的 **`庫存.xlsx`** 拖曳上傳。
   - 在匯入位置選擇 **「取代目前工作表」**，點擊 **「匯入資料」**。
   - **⚠️ 檢查要點**：請確認左下角的工作表（分頁）名稱是否精確為 **`Inventory list`** (注意大小寫，結尾不要有空格)。
   - 此時，您會看到欄位包含 `需要再訂購`、`庫存 ID`、`名稱`、`單價` 等列。

---

## 💻 第二階段：部署雲端 API 後端 (Google Apps Script)

### 步驟 2：複製貼上後端程式碼 (GAS)
1. 在剛剛建立的「庫存管理系統」試算表上方選單中，點擊 **「擴充功能」 > 「Apps Script」**。
2. 開啟瀏覽器編輯器後，將預設的 `function myFunction() {}` 全部刪除。
3. 打開本資料夾底下的 **`gas_script.gs`** 檔案，將裡面的**所有程式碼複製**。
4. 將程式碼貼入 Google Apps Script 編輯器中。
5. **🔐 設定安全驗證密碼（強烈建議）**：
   - 在程式碼的最頂部，有一行 `const ADMIN_PASSWORD = "admin";`。
   - `admin` 是系統的預設密碼。為了防止他人取得您的 API 網址後隨意修改您的庫存，您可以將其改為您個人的專屬密碼（例如 `const ADMIN_PASSWORD = "mySecrEt99";`）。
6. 點擊編輯器上方工具列的「**儲存**」💾 圖示（或按快捷鍵 Ctrl+S / Cmd+S），並將專案名稱命名為「**InventoryAPI**」。

### 步驟 3：部署並取得 API 網址 (極度重要！)
這是讓前端網頁能夠透過網路讀寫 Google 試算表資料的關鍵步驟：
1. 點擊右上角的藍色按鈕 **「部署」 > 「新增部署作業」**。
2. 點擊「選取類型」旁邊的齒輪圖示 ⚙️，勾選 **「網頁應用程式」**。
3. 填入以下欄位設定：
   - **說明**：`V1.0.0`
   - **執行身分**：選擇 **「我」** (您的 Google 帳號)。
   - **誰可以存取**：**務必選擇「所有人」** (這樣前端才能跨網域讀寫 API)。
4. 點擊 **「部署」** 按鈕。
5. **授權存取**（初次部署會要求帳號權限）：
   - 點擊「**授權存取**」，選擇您的 Google 帳號。
   - 若出現安全警告「Google 尚未驗證此應用程式」，請點擊左下角的「**進階**」 > 「**前往 InventoryAPI（不安全）**」。
   - 點擊「**允許**」。
6. 部署成功後，會看到 **「網頁應用程式網址」 (Web App URL)**。
7. **📋 請將這串網址完整複製並妥善保存**，我們在下一階段馬上會用到！

> [!WARNING]
> **⚠️ 未來修改 GAS 密碼或程式碼的大魔王注意事項：**
> 如果您未來修改了 Google Apps Script 中的任何程式碼（例如修改了 `ADMIN_PASSWORD` 密碼常數），**僅僅點擊「儲存」是絕對不會在線上生效的！** 
> 
> 您必須完成以下步驟，否則線上跑的永遠是舊程式碼：
> 1. 點擊右上角「部署」 > 「**管理部署作業**」。
> 2. 點擊右上角的 **「編輯」** (鉛筆圖示 ✏️)。
> 3. **關鍵步驟**：在「版本」下拉選單中，**一定要選擇「建立新版本」**！
> 4. 點擊右下角的 **「部署」**。

---

## 🎨 第三階段：使用 Google AI Studio 免安裝快速生成前端

在這個階段，我們將直接使用 Google AI Studio，透過老師分享的 Prompt，不需要在您的電腦上下載 Node.js，也不需要執行複雜的 `npm` 指令，就能獲得一個雙擊即用的 `index.html` 網頁！

### 步驟 4：將 Prompt 輸入 Google AI Studio 生成代碼
1. 開啟 [Google AI Studio](https://aistudio.google.com/)。
2. 建立一個全新的對話。
3. **複製下方備份的 Prompt**，並將裡面的 `[請填入你的 GAS 網址]` 替換為您在 **第一階段步驟 3** 複製的網頁應用程式網址。
4. 點擊下方的 **「Run」**（生成）按鈕，AI 將會為您產生一個**完整的單一 `index.html` 網頁程式碼**！

> [!TIP]
> **💡 老師為您準備的 AI Studio 完整 Prompt 內容**：
> 請點開下方展開，將裡面的 Prompt 內容完整複製到 AI Studio 中：
> <details>
> <summary>點擊展開：老師設定好的 AI Studio 庫存管理 Prompt 內容</summary>
> 
> ```text
> 請幫我建立一個精美的「雲端智慧庫存管理系統」前端網頁。
> 為了讓使用者雙擊檔案即可立即運行，請將所有的邏輯都集中在一個「單一的 index.html 檔案」中！
> 
> 【技術要求】
> 1. 請在 HTML 內使用 CDN 載入 React 18、ReactDOM 18、Babel Standalone（用於在瀏覽器端解析 JSX 語法）以及 Tailwind CSS CDN。
> 2. 所有的 TypeScript/JavaScript 邏輯、型別定義、元件與 App 整合元件全部寫在同一個 <script type="text/babel"> 區塊中。
> 
> 【API 規格與連線邏輯】
> 我已經建立好 Google Apps Script 的 API 後端，網址如下：
> [請填入你的 GAS 網址]
> 
> 1. 當網頁加載時，發送 GET 請求，必須於網址附加密碼參數，如 ?password=YOUR_PASSWORD（為了安全與避開快取，建議也加上 timestamp 隨機參數），否則會被拒絕讀取。後端回傳格式如下：
>    {
>      "status": "success",
>      "data": [
>        {
>          "needReorder": false,
>          "id": "IN0001",
>          "name": "電子天平",
>          "description": "高精度實驗室電子天平",
>          "price": 4500,
>          "quantity": 15,
>          "value": 67500,
>          "reorderPoint": 5,
>          "reorderTime": 7,
>          "reorderQty": 10,
>          "discontinued": false
>        }
>      ]
>    }
> 
> 2. POST 請求：傳送 JSON 物件，必須包含 password 欄位以完成驗證，且支援 3 種 action：
>    - 新增：{"action": "create", "password": "YOUR_PASSWORD", "data": {"name": "新商品", "description": "描述...", "price": 100, "quantity": 10, "reorderPoint": 5, "reorderTime": 3, "reorderQty": 20, "discontinued": false}}
>    - 修改：{"action": "update", "password": "YOUR_PASSWORD", "data": {"id": "IN0001", "name": "修改商品名稱", "description": "...", "price": 120, "quantity": 8, "reorderPoint": 5, "reorderTime": 3, "reorderQty": 20, "discontinued": false}}
>    - 刪除：{"action": "delete", "password": "YOUR_PASSWORD", "data": {"id": "IN0001"}}
> 
> 【UI 與元件設計要求】
> 請在設計上追求極簡優雅的現代風格（例如使用卡片式佈局、細緻的陰影、平滑的 transition 動畫、柔和的漸層配色）。請使用 Tailwind CSS 打造一個高商業質感的「暗黑模式 (Dark Mode)」或「可切換雙主題」儀表板：
> 
> 1. 儀表板數據指標 (KPI Cards)：
>    - 總庫存品項數（庫存商品個數）
>    - 庫存資產總價值（所有商品的 price * quantity 總和，格式化為台幣 NT$ 貨幣顯示）
>    - 安全庫存補貨警示（統計庫存數量 <= 再訂貨點 且未停產的商品項目數，以醒目橙色/黃色發光顯示）
>    - 停產商品項目（統計 discontinued 為 true 的商品數）
> 
> 2. 搜尋、篩選與排序區塊：
>    - 搜尋欄位：可即時透過 ID、名稱或描述搜尋商品。
>    - 篩選分頁標籤：提供「全部品項」、「充足」、「需補貨⚠️」以及「已停產🚫」按鈕切換。
>    - 表頭排序：點擊表格標題（例如單價、數量、總價值），能對庫存清單進行遞增或遞減排序。
> 
> 3. 庫存填單與修改表單 (InventoryForm 元件)：
>    - 欄位包含：商品名稱、規格描述、單價、庫存數量、再訂貨點、每次再訂貨數量、補貨時間、是否停產（開關切換或 checkbox）。
>    - 【即時估算功能】當使用者輸入「單價」與「數量」時，表單下方會即時顯示「預估庫存價值：NT$ XXX」；若庫存數量低於再訂貨點，會即時顯示「⚠️ 庫存已低於安全水位」的黃色警告。
>    - 送出時呼叫 POST API，並在新增/修改後，延遲 1.5 秒自動 GET 刷新數據庫。
> 
> 4. 商品列表表格 (InventoryTable 元件)：
>    - 以精緻 Table 佈局展示庫存商品，包含狀態 badge（正常供應、需補貨、已停產）、庫存 ID、名稱、描述、單價、數量、總價值。
>    - 提供「編輯」與「刪除」按鈕，點擊編輯時會將商品資料帶入表單並平滑滾動至頂部；點擊刪除時會彈出極具質感的「自訂確認模態對話框（Modal）」以防誤刪。
> 
> 5. 零設定 API 面板與訊息反饋：
>    - 提供 Toast 訊息提示元件，在 API 連線成功、新增/修改/刪除完成時滑出並在數秒後自動淡出。
>    - 右上角提供設定按鈕，點擊可開啟彈出視窗設定 GAS 的 Web App 網址與安全防護密碼（密碼也須存至 LocalStorage），讓使用者雙擊打開網頁就能透過 UI 自訂連接。
> 
> 請直接給我這一個 index.html 檔案的完整程式碼，並將 API 網址與預設密碼寫死在程式碼的預設變數中。
> ```
> </details>

---

## 🚀 第四階段：在本地雙擊即刻運行！ (僅需 3 步)

當 AI 為您生成了單一 HTML 的程式碼後，您只需要完成以下極簡步驟，就能直接運行：

1. **新建檔案**：
   在大專案資料夾中，點擊滑鼠右鍵「**新增**」 > 「**文字文件**」，並將該檔案重新命名為 **`index.html`** (請確保副檔名是 `.html` 而不是 `.txt`。Mac 用戶可直接在文字編輯器中建立並儲存為 html)。
2. **貼上並儲存**：
   用記事本或任何程式碼編輯器打開 `index.html`，將 AI 生成的**全部網頁程式碼複製並貼進去**，接著存檔儲存！
3. **滑鼠雙擊打開**：
   用滑鼠**雙擊打開 `index.html` 檔案**。瀏覽器會立刻載入一個極具質感的庫存管理系統前端，並自動跳出 API 與安全設定對話視窗。
   
   請在此處貼上您的 **API 網頁應用程式網址**，以及您在第二階段步驟 2 中設定的 **安全驗證密碼**（若無修改則預設為 `admin`），點擊儲存，即可順利與您的 Google 試算表進行即時安全通訊！🎉

---

## ✨ 系統亮點功能介紹

- 💡 **四大核心數據指標卡 (KPI)**：
  - **總庫存品項數**：即時統計庫存總商品種類。
  - **庫存資產總價值**：以 `單價 * 數量` 計算，即時展示總庫存資產額。
  - **安全庫存補貨警示**：若商品數量低於設定的「再訂貨點」，會自動將其標記為「需補貨 ⚠️」並計入警示卡中。
  - **停產商品項目**：統計目前已經停產的品項數量。

- 🎨 **暗黑 / 亮光主題切換**：
  - 點擊頂部右上角的「太陽/月亮」圖標，可在精心調配的**迷霧藍暗黑模式**與**簡約亮白模式**之間平滑切換。

- ⚡ **雙向即時運算表單**：
  - 在新增或編輯商品時，只要輸入「單價」與「庫存數量」，表單下方的微型小工具會**即時顯示估算總價值**。
  - 當您輸入的「數量」低於「再訂貨點」時，表單會即時彈出**黃色警告**，提醒您注意庫存不足！

- 🔍 **高效搜尋與多重篩選**：
  - **即時搜尋**：輸入關鍵字，可即時過濾 ID、商品名稱或規格描述。
  - **快速篩選標籤**：可一鍵切換查看「全部品項」、「充足 ✅」、「需補貨 ⚠️」或「已停產 🚫」商品。
  - **表頭排序**：點擊表格標題（例如單價、數量、總價值），能對庫存清單進行正序或倒序排列。

- 🛑 **自訂模態視窗與 Toast 提示**：
  - 捨棄了瀏覽器難看的預設彈出視窗，全面採用毛玻璃磨砂效果的「二次刪除確認框」與右上角「自動淡出 Toast 訊息通知」，為使用者帶來極致的 UX 體驗。