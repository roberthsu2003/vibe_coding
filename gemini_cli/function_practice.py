# -*- coding: utf-8 -*-

"""
這是一個練習 Python 函式定義與實作的檔案。
請完成以下五個函式的實作。
"""

def calculate_circle_area(radius):
    """
    計算並回傳圓形的面積。

    圓面積公式為 pi * r^2，請使用 3.14159 作為 pi 的近似值。

    Args:
        radius (int or float): 圓形的半徑。

    Returns:
        float: 計算出的圓形面積。
    """
    pass

def is_palindrome(text):
    """
    檢查一個字串是否為迴文。

    迴文是指一個字串從前面讀和從後面讀都一樣，忽略大小寫和空格。
    例如："A man a plan a canal Panama" 是一個迴文。

    Args:
        text (str): 要檢查的字串。

    Returns:
        bool: 如果字串是迴文，則回傳 True，否則回傳 False。
    """
    pass

def find_max_in_list(numbers):
    """
    找出並回傳一個數字列表中的最大值。

    如果列表是空的，應該回傳 None。

    Args:
        numbers (list of int or float): 一個數字列表。

    Returns:
        int or float or None: 列表中的最大值，或在列表為空時回傳 None。
    """
    pass

def count_vowels(sentence):
    """
    計算一個句子中母音 (a, e, i, o, u) 的數量。

    這個計算應該不區分大小寫。

    Args:
        sentence (str): 要分析的句子。

    Returns:
        int: 句子中母音的總數。
    """
    pass

def reverse_string(s):
    """
    將輸入的字串反轉。

    例如：輸入 "hello"，應回傳 "olleh"。

    Args:
        s (str): 要反轉的字串。

    Returns:
        str: 反轉後的字串。
    """
    pass

def is_prime(number):
    """
    檢查一個正整數是否為質數。

    質數是指除了 1 和它本身以外，沒有其他正因數的自然數。
    1 不是質數，2 是質數。

    Args:
        number (int): 要檢查的正整數。

    Returns:
        bool: 如果是質數，則回傳 True，否則回傳 False。
    """
    pass

def calculate_factorial(n):
    """
    計算一個非負整數的階乘。

    階乘的定義為 n! = n * (n-1) * (n-2) * ... * 1。
    0 的階乘定義為 1。

    Args:
        n (int): 一個非負整數。

    Returns:
        int: n 的階乘。
    """
    pass

def celsius_to_fahrenheit(celsius):
    """
    將攝氏溫度轉換為華氏溫度。

    轉換公式為：F = (C * 9/5) + 32。

    Args:
        celsius (int or float): 攝氏溫度。

    Returns:
        float: 轉換後的華氏溫度。
    """
    pass

def merge_and_sort_lists(list1, list2):
    """
    合併兩個數字列表，並回傳一個新的已排序列表。

    不應修改原始的兩個列表。

    Args:
        list1 (list of int or float): 第一個數字列表。
        list2 (list of int or float): 第二個數字列表。

    Returns:
        list: 一個包含兩個列表所有元素並已排序的新列表。
    """
    pass

def count_word_frequency(text):
    """
    計算一個字串中每個單詞出現的頻率。

    應忽略大小寫，並將標點符號視為單詞的一部分或進行處理。
    回傳一個字典，其中鍵是單詞，值是該單詞出現的次數。

    Args:
        text (str): 要分析的文字。

    Returns:
        dict: 一個記錄每個單詞出現頻率的字典。
    """
    pass






# --- 學生練習區結束 ---


